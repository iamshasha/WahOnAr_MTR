# 遷移至 MTR 4

這份文件記錄把華安亞伺服器由這個 MTR 3 分支遷移到 MTR 4 的計劃。它是寫來在決定之前先讀的，
而且刻意把會出錯的地方寫清楚。

以下所有事情都**尚未發生**。伺服器現時仍然執行 MTR 3，而且會一直執行到下面每一個問題都有人滿意的答案為止。

[English version](MIGRATION.md)

## 為甚麼要搬

不是因為 MTR 4 比較新。是三個具體理由。

**擴充模組已經搬走了。** JCM 的分界乾淨而且公開：v1.2.2 是 MTR 3 的最後一個版本，
而每一個 v2.x 版本都只支援 MTR 4。腳本引擎 —— 令自訂 PIDS 行為成為可能的那一部分 —— 只存在於 v2。
MSD、London Underground、TransitManager 和 Interference 的處境相同。
留在 MTR 3 並不代表等它們追上來，而是它們永遠不會追上來，因為我們現在執行的版本就是為我們而存在的最後一個。

**這個分支加的東西，MTR 4 大部分已經有了。** 這是我們真的去讀原始碼（而不是憑猜測）之後的意外發現。
模擬邏輯住在
[Transport-Simulation-Core](https://github.com/Minecraft-Transit-Railway/Transport-Simulation-Core)，
一個獨立的 MIT 授權專案，而它已經有：

| 這個分支帶著的東西 | MTR 4 的對應功能 |
| --- | --- |
| 嚴格班表（真實時鐘開出時間） | `Depot.setUseRealTime` / `getRealTimeDepartures`，可在車廠介面編輯 |
| 拉長停站時間以扣住早到的列車 | `Siding.getEarlyVehicleIncreaseDwellTime` |
| 遲到列車的追時 | `Siding.getDelayedVehicleSpeedIncreasePercentage` 與 `…ReduceDwellTimePercentage` |
| 班次帳簿，防止兩架車搶同一班次 | 班次在產生時就已分配給唯一一條側綫，所以根本不可能發生 |
| — | OneBusAway 介面與線上系統地圖，兩樣都已經做好 |

最後一行才是重點。MTR 4 不像我們那樣在派車嗰一刻先去搶班次；
它在產生班表時就把每一個班次交給恰好一條側綫，而且事先確認過會有車及時回來執行。
這個月這個分支修過的每一個重複派車、幽靈班次、重複到站的問題，
在那個設計下都是不會存在的問題。遷移主要是刪東西。

**維護這個分支本身就是負擔。** 我們改的每一處，日後都要再推導一次，而且全部由我們自己扛。

## 為了令 MTR 3 保持穩定，我們先做甚麼

現正進行的工作都在 MTR 3 上面，而且無論遷移是否成事都有用。

- **把班表邏輯搬出與 Minecraft 耦合的類別。** `DepartureLedger` 就是樣板：
  以普通型別做普通運算，而且它的檢查不用開遊戲就能跑。下一個是到站時間推算。
  這件事本身就值得做 —— 那段程式碼造成過四個錯誤，而且完全沒有測試覆蓋 —— 順帶令它變得可移植。
- **任何 MTR 4 的程式碼都不會靠近出貨分支。** ANTE 上的 MTR 4 工作（Compound Creator 與 Displacement Tool 的移植）
  住在自己的分支、用自己的更新紀錄，令 MTR 3 這條線任何時候都可以出貨。
- **每一個版本在發佈之前都會先啟動測試**，而且發佈的檔案會與測試過的那一個逐位元組比對。

## 一定會出事的那一項：軌道類型

MTR 4 在首次載入時會轉換 MTR 3 的世界。它的轉換器帶著一份寫死的 MTR 3 軌道類型副本
（`org.mtr.legacy.data.DataFixer.RailType`），並且按名稱解析已儲存的軌道：

```java
return EnumHelper.valueOf(DataFixer.RailType.WOODEN, rail_type);
```

**任何它不認識的軌道類型都會變成 WOODEN —— 20 km/h。** 那份清單只有上游的十六種，沒有別的。它沒有：

- 由 High Speed Rails 擴充吸收過來的那五種（450、500、600、700、800 km/h），以及
- ANTE 那大約 640 種合成的 `P###` 類型，每 km/h 一種 —— 那就是 ANTE 現時讓一條軌道有任意速度的做法。

所以在我們遷移那一日，如果沒有事先準備，
伺服器上每一條高速軌道都會靜靜地變成慢速木軌。不是壞掉，不是消失，只是全部都錯，
而且原本的速度已經沒有了，因為類型名稱是唯一記錄過它的地方。

有兩件事處理這個問題，而第一件已經做好：

1. **每一條受影響的軌道現時都會把自己的速度記在** 軌道的 `speedLimitKmh` 欄位，而不只是記在類型名稱裡。
   這在今日不花任何代價，而且代表就算名稱沒有了，那個數字仍然在。
2. **轉換器是可以改的。** `Transport-Simulation-Core` 是 MIT 授權，
   而 MTR 4 是透過 `flatDir` 以 `libs/` 裡一個普通 jar 的形式引用它，
   所以自己建置一份放進去並不困難。
   教它的軌道類型表認得我們那五種、並且讓它讀取已儲存的每軌速度，
   對一份我們有權修改的原始碼來說是很小的改動。

這兩件事都不是可以草率遷移的理由。它們是「要先用真實世界的副本測試過轉換器再遷移」的理由。

## 轉換器由我們自己寫，而且它們只是普通程式

在我們真的去看兩邊如何儲存一個世界之前，這一直被當成最難的部分。
兩邊都把 MessagePack 檔案放在 `<world>/mtr/<namespace>/<dimension>/` 下面，按類型分資料夾 ——
而 MTR 3 有的每一個資料夾，名稱都一樣：

```
MTR 3   stations platforms sidings routes depots lifts rails
MTR 4   stations platforms sidings routes depots lifts rails  + homes landmarks
```

所以任何一個方向的轉換器，都只是一個獨立的 MessagePack 轉 MessagePack 程式。
不是模組，不是 mixin，classpath 上不需要 Minecraft，測試它也不用開遊戲。
它可以對著真實世界的副本執行，輸出可以逐項比對。
這就是「可以預演的遷移」和「只能相信它會成功的遷移」之間的分別。

**正向，MTR 3 轉 MTR 4。** MTR 4 自己的 `org.mtr.legacy` 已經做了大部分對應，
本身就是欄位對照的現成範例。它對我們來說做錯的地方，是上面那張軌道類型表，
以及它從來不讀我們現在寫上去的每軌速度。兩者都是對一份我們有權修改、
而且 MTR 4 以 `libs/` 裡普通 jar 的形式引用的原始碼所做的小改動。

**反向，MTR 4 轉 MTR 3。** 值得寫，而且值得**第二個**寫：
正向那一遍才會確立反向所需要的欄位對照，
倒過來做就等於猜兩次。它可以刻意有損 ——
MTR 4 裡沒有 MTR 3 對應物的欄位（誤點追蹤、homes、landmarks）直接丟棄即可。
必須完整保留的是路軌幾何和班表，而那兩樣是一對一對應的。

## 如果出事

**把模組退回去很容易。** 每一個發佈過的版本都仍然在封存 CDN 上，
而 `map.json` 以版本號和 SHA-256 釘住每一個。把條目改回去，
每一個客戶端下次啟動就會拿回舊的檔案。甚麼都不用重新建置。

**把世界退回去需要那個反向轉換器**，這也是為甚麼它在清單上，而不是被當成不可能。
MTR 4 在首次載入時會把 MTR 3 的資料改寫成自己的格式，而它自己沒有辦法改回去。

即使有了那個轉換器，第一道防線仍然是**在第一次啟動 MTR 4 之前立即備份世界，並且要載入過那份備份來驗證它**。
一份沒有人開過的備份是猜測，不是備份。
轉換器買回來的，是那份備份與有人發現問題之間的那段遊玩時間；
其餘一切由備份覆蓋，而且不花任何代價。我們打算的次序是：

1. 關閉伺服器，複製世界。
2. 在一部後備的 MTR 3 伺服器上載入那份副本，確認鐵路真的能跑 —— 會出車、班表守得住。
3. 到這一步之後，才讓 MTR 4 碰真正的世界。

如果遷移之後有問題，我們還原那份副本並從 CDN 取回 MTR 3 的模組組合；
或者，如果之後累積的東西多到不捨得丟，就對那個 MTR 4 世界執行反向轉換器。

比這兩者都好的是：**根本不需要用到它們。**
在一份世界副本上並行執行 MTR 4，執行到有信心為止，
就能把遷移由一場實驗變成一次切換，
並且把「還原」變成「繼續玩那部從來沒有停過的 MTR 3 伺服器」。
那條路甚麼都不會失去，而且在沒有理由不這樣做的情況下，就應該走那一條。

## 如果有擴充模組或列車包不支援 MTR 4

**列車包是好的情況。** MTR 4 專門帶了 `org.mtr.legacy.resource` 來讀取 MTR 3 的列車、軌道、標誌和物件資源，
所以現有的列車包預期可以載入。「預期」不等於「已驗證」——
伺服器用到的每一個列車包都會先在測試實例上載入，
任何載入失敗的都會被逐個列出名字，而不是由玩家發現。

**擴充模組分三種情況：**

- *已經在 MTR 4 上* —— JCM、MSD、London Underground、TransitManager、Interference。
  這些會拿到比我們今日能執行的更新的版本，這是搬過去的理由，而不是搬過去的風險。
- *已被吸收或取代* —— ANTE 加的東西大部分 MTR 4 已經有了。
  已知的缺口是 **Compound Creator** 和 **Displacement Tool**，MTR 4 沒有對應的東西；
  MTR 4 的橋樑與隧道建造器不能逐層挑選方塊，而且沒有復原功能。
  這兩樣正在當作獨立移植來評估。路線建立器不在其中：它在這個伺服器上的使用紀錄是零。
- *已死* —— High Speed Rails 擴充，已經從 Modrinth 和 GitHub 移除，無人維護。
  它的軌道類型已經吸收進這個分支，所以移除那個模組不會失去任何東西，也沒有東西需要移植。

任何不屬於以上三種的東西都是阻礙，而發現一個就是停下來重新規劃的理由，而不是硬推的理由。
測試實例存在的目的，就是要在伺服器之前先發現它們。

## 目前狀態

刻意保持未定。工程上的理由現在已經清楚到足以行動；
但伺服器是否要承受這段停機時間和風險，並不是一個工程決定。
